# ADR 0001: Java 21

Use Java 21 for the core CLI.

Java keeps distribution straightforward on macOS, provides enough standard library support for process execution and flat-file state, and avoids framework gravity. Phase 1 uses no runtime dependencies.

